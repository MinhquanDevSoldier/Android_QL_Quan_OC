<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appqlquanoc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppQLQuanOc">
        <activity android:name=".acitvity_bill_layout"></activity>
        <activity android:name=".acitvity_manage_account_layout" />
        <activity android:name=".activity_manage_table" />
        <activity android:name=".activity_manage_food_layout" />
        <activity android:name=".activity_login_layout">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter></activity>
        <activity android:name=".activity_select_food_layout" />
        <activity android:name=".TableLayout">

        </activity>
        <activity android:name=".MainLayout"></activity>
    </application>

</manifest>